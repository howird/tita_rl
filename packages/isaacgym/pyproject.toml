[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "isaacgym"
dynamic = ["version", "description", "authors", "requires-python", "dependencies", "license"]
